# classic-imap-smtp-mcp — Single Account Setup
#
# TOML-Konfiguration wird nicht mehr unterstützt.
# Der Server wird ausschließlich über Umgebungsvariablen konfiguriert.
#
# Siehe .env.example für alle verfügbaren Variablen.
# Siehe docs/config.md für die vollständige Referenz.
#
# Kurzform (für Gmail, Outlook, iCloud usw. reichen user + pass):
#   CLASSIC_USERNAME=you@gmail.com
#   CLASSIC_PASSWORD=your-app-password
